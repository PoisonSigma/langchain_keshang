/*

查看系统的external实现：
进入自己安装的flutter sdk目录  flutter/bin/cache/dart-sdk/lib
终端输入：
grep -n "class 类名" -r ./* --color=auto

例如：系统的Object：
  flutter/bin/cache/dart-sdk/lib
  下面执行
grep -n "class Object" -r ./* --color=auto
找到里面的类文件


grep -n "class List" -r ./* --color=auto


 // grep -n "class Object" -r ./* --color=auto
//grep -n "class List" -r ./* --color=auto
*/
